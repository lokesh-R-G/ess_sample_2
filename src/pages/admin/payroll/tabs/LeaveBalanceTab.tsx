import React from 'react';

const LeaveBalanceTab = ({ month, branchId }: { month: string; branchId: string }) => {
  return <div className="p-4">Leave Balance UI for {month} {branchId}</div>;
};
export default LeaveBalanceTab;
